---
globs:
  - "**/*.{ts,tsx,js,jsx,mjs,cjs}"
---
# Require each change to justify its complexity

Before accepting code, identify the requirement it satisfies, the existing code that could serve the same purpose, what the change can remove, and whether a simpler version preserves the required behavior.
