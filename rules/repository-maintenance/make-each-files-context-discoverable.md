---
globs:
  - "**/*"
---
# Make each file's context discoverable

A developer must be able to identify a file's owning feature, allowed dependencies, execution environment, public contract, and test location from the file's path and nearby declarations, without searching the entire repository.
