filenames should be distinct and descriptive.
they should be as simple as possible.
they should accurately describe the code entities within them.

files are also the module boundary in JavaScript and TypeScript which means all modularity concerns should also be handled within the file boundary.