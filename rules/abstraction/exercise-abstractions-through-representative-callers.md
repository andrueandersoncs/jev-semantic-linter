---
globs:
  - "**/*.{ts,tsx,js,jsx,mjs,cjs}"
---
# Exercise abstractions through representative callers

Implement at least one realistic end-to-end use before polishing an abstraction's interface. When claiming reuse, test materially different intended uses; repeated special cases are a reason to reconsider the boundary.
